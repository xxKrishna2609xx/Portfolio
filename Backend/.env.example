# GitHub Personal Access Token (PAT) for authenticated high-limit requests
GITHUB_PAT=

# MongoDB connection URI (falls back to local database if blank)
MONGODB_URI=mongodb://localhost:27017

# Gemini API Key for chatbot integrations
GEMINI_API_KEY=
